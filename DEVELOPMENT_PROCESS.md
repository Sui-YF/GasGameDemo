# 《修仙大战外星人》Demo 开发流程与技术约定

## 1. 项目目标

本项目是一个 1～2 人合作的 Unreal Engine 动作游戏 Demo：

- 主角使用兰芳角色资源。
- 单人时可自由切换持剑与御剑姿态。
- 双人时使用 Listen Server 联机。
- 战斗、属性、技能消耗和技能装配以 GAS 为核心。
- 小兵与 Boss 必须使用 Behavior Tree。
- Demo 地图提供刷兵、击杀目标、Boss 战和胜负结算。
- 玩家具有背包、换装、技能树、升级、存档、设置和自定义名字等基础系统。

开发目标不是一次性做成完整商业游戏，而是先建立一条稳定、可扩展、可联机验证的纵向玩法链路。

## 2. 总体开发方法

每个功能按以下顺序开发：

1. 从 GDD 中提取一项玩家能感知的目标。
2. 检查项目已有代码、资产和蓝图，避免重复实现或覆盖现有内容。
3. 确定功能属于 C++、GAS、Blueprint、AnimBP、Widget 还是 DataTable。
4. 先实现最短的完整链路，而不是一次堆很多孤立功能。
5. 编译 C++，运行编辑器脚本生成或配置资产。
6. 启动独立游戏，依据运行日志和截图验证。
7. 修复加载顺序、联网权威、动画同步和输入冲突。
8. 功能稳定后，再增加表现、特效、音效和正式排版。

例如攻击不是只做到“按键能播放动画”，而是完成：

`Enhanced Input → GAS 激活 → 资源消耗 → 动画 → 命中帧 → 服务端检测 → Gameplay Attribute 扣血 → 死亡/经验/任务更新`

## 3. 技术职责划分

### C++

负责不能信任客户端、需要复用或需要稳定运行的规则：

- Listen Server 权威判定与属性复制。
- GAS AbilitySystemComponent 和 AttributeSet。
- 伤害、资源消耗、资源恢复与升级。
- 背包、换装、技能装配和存档数据。
- 刷兵器、Boss 阶段和战斗状态。
- AI Controller 与 Behavior Tree Task。
- UI 数据接口、菜单旅行和设置接口。

### Gameplay Ability System

负责技能规则：

- 技能是否允许发动。
- 体力或灵力是否足够。
- 技能参数、伤害、距离和 AOE 半径。
- 技能槽装配和 Ability 授予。
- 后续的冷却、状态标签、Buff、Debuff 和 Gameplay Cue。

### Animation Blueprint

负责持续动画状态，不承担战斗数值：

- 普通姿态的待机、走、跑和跳跃。
- 御剑姿态的待机、走、跑和跳跃状态继承。
- 根据速度、空中状态和姿态选择动画。
- 后续通过 Slot/Montage 叠加攻击、受击和倒地。

不使用 Tick 中反复 `PlayAnimation` 作为最终移动方案，因为这会绕过 AnimBP 状态机。

### Blueprint

负责资产配置和关卡组合：

- 玩家、敌人和 Boss 的 Mesh、AnimBP 与默认参数。
- 关卡中刷兵 Box、导航区域和任务 Actor 的摆放。
- 特效、音效、阶段事件和镜头表现连接。
- 不在蓝图中直接实现客户端扣血等权威规则。

### DataTable

负责适合批量平衡的数据：

- 玩家基础数值。
- 小兵、Captain 和 Boss 数值。
- 刷兵配置。
- 技能伤害、范围、消耗、动画和装配信息。

修改平衡数据时不需要重新编译 C++。

### Widget Blueprint

负责 UI 控件树和最终排版：

- C++ 提供功能和数据接口。
- Widget 中放置命名控件并连接 OnClicked。
- 正式锚点、尺寸、字体、颜色和动效由 UI 排版阶段处理。
- 联机属性只读复制数据，不能让客户端 Widget 直接改服务器状态。

### 编辑器 Python

Python 只用于开发期自动化：

- 创建或配置 `.uasset`。
- 批量生成 DataTable、Input Action、Widget 控件和 AnimNotify。
- 调用项目的 C++ Editor Builder。
- 批量验证资产引用。

打包后的游戏不依赖 Python；运行时玩法仍由 C++、GAS、Blueprint 和 AnimBP 完成。

## 4. 联网设计

项目采用 Listen Server，核心原则是“客户端表达意图，服务器决定结果”。

### 服务器负责

- 技能是否成功激活。
- 体力、灵力和生命的修改。
- 攻击检测与伤害结算。
- 小兵、Boss 的生成和死亡。
- 击杀计数、经验、升级、技能点和胜负状态。
- 救援、技能解锁和换装请求验证。

### 客户端负责

- 采集本地输入。
- 播放本地相机和 UI 表现。
- 显示服务器复制的角色、属性、Boss 血量和任务状态。

### 同步策略

- 角色和敌人开启移动复制。
- PlayerState 保存需要跨 Pawn 存续的 GAS、成长和装备数据。
- BattleDirector 复制关卡阶段、击杀数、Boss 血量和胜负状态。
- 战斗动画通过服务器多播播放；伤害仍只在服务器结算。

## 5. 战斗与动画处理

### 左右键攻击

- Enhanced Input 将鼠标左右键映射到 GAS 技能槽。
- 技能激活后扣除体力或灵力。
- 动画命中 Notify 触发服务端范围检测。
- 为避免 Notify 因动画模式切换漏掉，服务器还使用命中时间计时器作为兜底。
- Notify 和计时器共享一次性标记，因此一次攻击最多结算一次。

### 边移动边攻击

- 攻击期间 CharacterMovement 保持权威。
- 动画 Root Motion 使用 IgnoreRootMotion，避免攻击位移抢走玩家移动。
- 攻击输入锁只限制下一次攻击，不阻止移动、旋转和跳跃。
- 最终目标是使用 AnimMontage + Slot，使攻击覆盖指定骨骼层。

### 持剑与御剑

- 持剑模式使用 `Female_AnimBP`。
- 御剑模式使用 `ABP_LanFang_FlyingSword`。
- 御剑 BlendSpace 使用 `Anim_FS_Standing / Walk / Run`。
- 进入使用 Equip，退出使用 UnEquip。
- 三把剑分别绑定原资源的 `Weapon_l / WEAPON_M / Weapon_r` 骨骼，位置由原动画驱动。

### 飞剑技能

- FS Attack 01、02、03 都按 AOE 规则检测目标。
- FS Attack 02 发动时寻找范围内最近敌人并朝向目标。
- 后续优化为真正的飞剑 Actor：服务器生成、追踪目标、碰撞命中、返回并进入对象池。

## 6. AI 与 Boss

### 小兵

- Box 检测玩家进入后开始刷兵。
- 玩家离开后暂停生成。
- 同时存活数量和击杀目标由 DataTable 配置。
- AI 必须通过 Behavior Tree 执行寻找目标、移动和攻击。

对刷兵加载顺序做了特殊处理：Spawner 可能比玩家 Pawn 更早 BeginPlay，因此服务器会在初始化后延迟扫描一次 Box 内玩家，避免漏掉第一次 Overlap。

### Boss

- Demo 暂时复用放大的 TPP Captain 作为 Boss 占位资源。
- 小兵波次完成后由服务器生成 Boss。
- 70% 和 35% 生命触发第二、第三阶段。
- 第二阶段加快攻击并提高伤害。
- 第三阶段扩大攻击范围并增加范围冲击。
- Boss 血量和阶段由服务器更新并复制到 HUD。
- Boss 死亡后 BattleDirector 切换 Results 并标记胜利。

正式机器人资源导入后，只替换 Mesh、Skeleton、AnimBP、Montage、碰撞体和特效，不重写战斗框架。

## 7. 成长、资源与技能树

### 资源恢复

- 体力每秒恢复 14。
- 灵力每秒恢复 7。
- 服务端每 0.25 秒更新一次，减少每帧网络与属性修改开销。
- 持剑技能主要消耗体力，飞剑技能主要消耗灵力。

### 经验升级

- 普通小兵奖励 25 经验。
- Captain 奖励 60 经验。
- Boss 奖励 500 经验。
- 升级需求为 `当前等级 × 100`。
- 每级生命上限 +15、体力上限 +10、灵力上限 +12。
- 升级时三项资源回满，并获得 1 技能点。

### 技能树

- Widget 已准备持剑 1～5、飞剑 1～3 的节点控件。
- 客户端通过 Server RPC 请求消费技能点。
- 后续需增加已解锁技能数组、前置节点、洗点成本和存档恢复。

## 8. UI 与流程

已准备的 Widget：

- Main Menu
- Lobby
- HUD
- Inventory
- Pause
- Settings
- Skill Tree
- Name Entry
- Result

HUD 已提供生命、体力、灵力、等级、经验、技能点、击杀数、阶段、Boss 血条和胜负控件。

设置页已准备分辨率、画质、渲染比例、全屏、VSync、音量、灵敏度、语言和按键重绑控件。当前画质设置接口已实现；音频需要正式 SoundClass/SoundMix 资产，运行时重绑还需完整实现。

## 9. 存档策略

当前存档槽：`CVAD_Profile_0`。

保存内容：

- 玩家名字。
- 等级、经验和技能点。
- 已装备技能。
- 当前装备。
- 后续可增加已解锁技能、设置和最好通关记录。

Listen Server 下，客户端请求服务器恢复权威成长数据，不能仅在本地 Widget 修改 PlayerState。

## 10. 性能优化考虑

### CPU 与动画

- 远处敌人使用 Visibility Based Anim Tick，屏幕外不持续刷新完整骨骼。
- 玩家装备使用 Leader Pose，避免每件装备独立计算动画。
- 攻击检测只在命中窗口执行，不每帧扫描。
- 资源恢复以 0.25 秒定时器更新，不放在 Tick。
- AI 决策由 Behavior Tree 管理，避免所有敌人写复杂 Actor Tick。

### 网络

- 生命、资源、阶段等结果由服务器复制，不复制客户端临时判断。
- 使用网络裁剪距离减少远距离 Actor 的复制成本。
- 只有服务器生成敌人和 Boss。
- UI 不发送高频 RPC。
- 鼠标朝向 RPC 当前为 Unreliable，后续可增加角度阈值和发送频率限制。

### 内存与生成

- Demo 先限制最多同时存在 4 个小兵。
- 后续大量刷兵应使用对象池，避免频繁 Spawn/Destroy。
- 技能与数值使用 DataTable，避免在多个蓝图中复制同一数据。

### 可见性

- 远处角色使用 Cull Distance 节省渲染，但不通过 HiddenInGame 让 Actor 逻辑消失。
- 网络裁剪与视觉裁剪分开配置。

## 11. 验证流程

每轮开发至少执行以下检查：

1. UnrealHeaderTool 和 C++ 编译通过。
2. Editor Python 脚本执行无 Traceback。
3. 检查 `.uasset` 是否生成并成功保存。
4. 独立游戏加载默认地图。
5. 从日志确认输入、技能装配、刷兵、BT 和阶段状态。
6. 截图检查相机、角色、剑和 HUD。
7. 最终进行 Listen Server 双进程测试：
   - 主机创建房间。
   - 客户端加入。
   - 双方移动、攻击和动画可见。
   - 伤害、经验、Boss 血量和胜负一致。
   - 客户端不能直接修改权威数据。

## 12. 已发现并修复的问题

- 移动方向错误：重新按相机 Yaw 分解前后与左右方向。
- 飞剑手工绑在脊柱位置错误：改为使用原动画武器骨骼。
- 御剑移动用 PlayAnimation 绕过 AnimBP：改为独立御剑 AnimBP 和 BlendSpace。
- 攻击 Notify 偶尔不结算：加入服务器计时兜底和一次性命中标记。
- 攻击阻止移动：忽略攻击 Root Motion，保留 CharacterMovement。
- Box 内出生不触发刷兵：延迟重新扫描初始玩家。
- Demo 数值被 Frontline DataTable 覆盖：增加独立 DemoPlayable 配置。
- UI 有资产但没有控件：批量生成命名控件骨架，排版留给 UI 阶段。
- 客户端技能点直接修改不安全：改为 Server RPC 请求。

## 13. 当前技术债务

以下功能已有框架，但仍需继续完成：

1. 攻击全面改为 AnimMontage + Slot。
2. 飞剑 02 使用独立追踪飞剑 Actor，而不只是自动朝向。
3. 小兵和 Boss 的攻击、受击、倒地和死亡动画。
4. 真正的机器人 Boss 资源与阶段技能特效。
5. 技能树的解锁数组、前置关系、洗点和存档。
6. Enhanced Input 运行时重绑、冲突检测和保存。
7. SoundClass/SoundMix 与音量设置。
8. 独立 `L_MainMenu` 和完整大厅准备流程。
9. 双人救援进度条、救援打断和单人护身符次数。
10. 双进程联机、模拟延迟和打包测试。

## 14. 推荐的后续开发顺序

1. Montage/Slot 攻击与受击表现。
2. 玩家攻击、敌人死亡和经验闭环回归测试。
3. Boss 专属动画、预警区和三阶段技能。
4. 技能树完整数据模型。
5. 设置、运行时重绑与存档。
6. 主菜单、名字输入、Listen Server 大厅和结果页。
7. 双人救援、失败与复活。
8. 正式地图、机器人资源、VFX、音频与 UI 排版。
9. 性能分析、网络延迟测试和打包验收。

## 15. 完成标准

Demo 达到以下条件才算真正完成：

- 单人可从主菜单进入、完成小兵波次、击败 Boss 并返回结果页。
- 双人可创建/加入 Listen Server，共同战斗、救援并同步结算。
- 持剑与御剑的移动、攻击、消耗和恢复正常。
- 背包、换装、技能树、升级、存档、名字和设置可以使用。
- 客户端不能通过 UI 或本地逻辑作弊修改权威属性。
- 无阻塞性崩溃、蓝图编译错误和关键资产丢失。
- 打包后的 Development Build 可以脱离编辑器运行。
